In the folder Accuracy test we have made the model to test iots accuracy.

In the folder Project we have implemented the model using simple python script and the model we gt best fit from Accuracy test.